TAG VER GIT

----------------------------
git add .
git commit -m "docs: align README and spec with querypath 1.0.0 pattern rules"
$hash = git rev-parse --short HEAD
git tag -a "v1.0.0-$hash" -m "Release v1.0.0 hash: $hash"
git push origin main --follow-tags
git push origin --tags
----------------------------
git tag -n
git tag -n5
git tag --sort=creatordate
git tag --sort=-creatordate

git log --oneline --graph --all --decorate