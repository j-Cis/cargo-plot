# CODE SNAPSHOT v:2026-39-AUT-1_0113140750

## Metadata

```text
├─ Katalog roboczy (CWD): A:/A-JAN/WIN-DOCS/REPO_OWN_RUST/GIT_j-Cis/cargo-plot@1
├─ Lokalizacje (scan_at): ["./src/"]
└─ Wzorce (match_pattern): []
📦 Zeskanowano fizycznie: 2 plików, 0 katalogów
```

## Structure

```plaintext
 ▣─┬ cargo-plot@1                   [6.3 KiB / 0 B]                    A:/A-JAN/WIN-DOCS/REPO_OWN_RUST/GIT 
   │                                                                   _j-Cis/cargo-plot@1/                
   └──┬ src                         [    0 B / 0 B]                                                        
1     ├──• cli.rs                   [      2.6 KiB] [2026-09-21 01:11] ./src/cli.rs                        
2     └──• main.rs                  [      3.7 KiB] [2026-09-21 01:11] ./src/main.rs                       
```

## Source Code Content

### [1] `./src/cli.rs`

```rust
use clap::{Args, Parser};

#[derive(Parser, Debug, Clone)]
#[command(name = "cargo", bin_name = "cargo", version, about, long_about = None)]
pub enum CargoCliRoot {
    #[command(name = "plot", about = "Szwajcarski scyzoryk do wizualizacji struktury projektu", version)]
    Plot(PlotArgs),
}

#[derive(Args, Debug, Clone)]
pub struct PlotArgs {
    // ==========================================
    // 🔍 OPCJE SKANOWANIA (querypath)
    // ==========================================
    /// Ścieżka katalogu do skanowania (domyślnie obecny katalog)
    #[arg(short = 'd', long = "dir", default_value = ".")]
    pub dir_to_scan: String,

    /// Wzorce wyszukiwania (np. "*.rs", "!target/**")
    #[arg(short = 'p', long = "pattern", visible_aliases = ["pat", "patterns"])]
    pub patterns: Vec<String>,

    /// Ignoruj wielkość liter przy dopasowywaniu wzorców
    #[arg(short = 'i', long = "ignore-case")]
    pub ignore_case: bool,

    // ==========================================
    // 🎨 OPCJE WYGLĄDU (querypath-fmt)
    // ==========================================
    /// Domyślna szerokość kolumny z nazwą pliku
    #[arg(long = "name-width", default_value_t = 25)]
    pub name_width: usize,

    /// Domyślna szerokość kolumny ze ścieżką
    #[arg(long = "path-width", default_value_t = 35)]
    pub path_width: usize,

    /// Wyłącz numerację plików w drzewie
    #[arg(long = "no-numeration")]
    pub no_numeration: bool,

    /// Użyj systemu SI (KB, MB) zamiast binarnego IEC (KiB, MiB) dla rozmiarów plików
    #[arg(short = 'B', long = "size-si")]
    pub size_si: bool,

    /// Wzorzec formatowania daty i czasu (używa tokenów temporal-fmt)
    #[arg(short = 'Y', long = "time-fmt", default_value = "YYYY-MM-MD hh:mm")]
    pub time_format: String,

    /// Zwiń/Ukryj pliki w widoku (pokazuj tylko katalogi) - przydatne do ogólnego widoku struktury
    #[arg(long = "dirs-only")]
    pub dirs_only: bool,

    // ==========================================
    // 📦 OPCJE EKSPORTU (querypath-snapshot)
    // ==========================================
    /// Ścieżka do katalogu, w którym zostanie zapisany wygenerowany raport Markdown
    #[arg(short = 'o', long = "out-dir")]
    pub out_dir: Option<String>,

    /// Tytuł raportu Markdown
    #[arg(short = 'u', long = "title", default_value = "CODE SNAPSHOT")]
    pub title: String,

    /// Maksymalny rozmiar pojedynczego pliku tekstowego (w bajtach) dołączanego do raportu (domyślnie 512 KiB)
    #[arg(long = "max-file-size", default_value_t = 524288)]
    pub max_file_size: u64,
}
```

### [2] `./src/main.rs`

```rust
mod cli;

use anyhow::Result;
use clap::Parser;
use cli::{CargoCliRoot, PlotArgs};

use querypath::QueryPath;
use querypath_fmt::{
    Column, DirWeightDisplay, Numeration, QueryPathFmt, StatsTemporal, StatsWeight, UnitSystem,
    WeightPrecision,
};
use querypath_snapshot::Snapshot;

fn main() -> Result<()> {
    // 0. Trik deweloperski do integracji z Cargo
    let mut args: Vec<String> = std::env::args().collect();
    if args.len() > 1 && args[1] != "plot" {
        args.insert(1, "plot".to_string());
    }
    // Jeśli wywołano puste `cargo plot`, dodajemy flagę pomocy
    if args.len() == 1 || (args.len() == 2 && args[1] == "plot") {
        if args.len() == 1 {
            args.push("plot".to_string());
        }
        args.push("-h".to_string());
    }

    // 1. Parsowanie flag CLI
    let CargoCliRoot::Plot(cli) = CargoCliRoot::parse_from(args);

    // 2. Wykonanie zadania
    run_engine(cli)?;

    Ok(())
}

fn run_engine(cli: PlotArgs) -> Result<()> {
    // =========================================================================
    // ETAP 1: SKANOWANIE (querypath)
    // =========================================================================
    println!("🔍 [cargo-plot] Skanowanie ścieżki: {}", cli.dir_to_scan);
    
    let mut scanner = QueryPath::new().scan_at([&cli.dir_to_scan]).keep_parent(true);

    if !cli.patterns.is_empty() {
        scanner = scanner.match_pattern(&cli.patterns);
    }
    if cli.ignore_case {
        scanner = scanner.ignore_case(true);
    }

    let results = scanner.run()?;

    println!(
        "📦 Znaleziono: {} plików, {} katalogów",
        results.scanned_files, results.scanned_dirs
    );

    // =========================================================================
    // ETAP 2: FORMATOWANIE I WIDOK (querypath-fmt)
    // =========================================================================
    let unit_sys = if cli.size_si { UnitSystem::Decimal } else { UnitSystem::Binary };
    
    let fmt = QueryPathFmt::new()
        .name_width(cli.name_width)
        .path_width(cli.path_width)
        .column_order_left([])
        .column_order_right([Column::Weight, Column::Temporal, Column::Path])
        .numeration(
            Numeration::new()
                .enabled(!cli.no_numeration)
                .numerate_dirs(false)
                .numerate_binaries(false)
                .start_from(1),
        )
        .stats_weight(
            StatsWeight::new()
                .enabled(true)
                .unit_system(unit_sys)
                .dir_display(DirWeightDisplay::Both) // Pokaże np. 12 MB / 140 MB
                .precision(WeightPrecision::Tenths),
        )
        .stats_temporal(StatsTemporal::new().enabled(true).pattern(cli.time_format));

    // Renderujemy i wypluwamy na ekran
    let rendered_tree = fmt.format(&results);
    println!("\n{}", rendered_tree);

    // =========================================================================
    // ETAP 3: EKSPORT (querypath-snapshot)
    // =========================================================================
    if let Some(out_dir) = cli.out_dir {
        println!("🚀 Generowanie migawki (Snapshot)...");
        
        let snapshot = Snapshot::new()
            .title(cli.title)
            .output_dir(out_dir)
            .version_pattern("WYYY-WW-SSS-D_hhmmssttqq")
            .file_name_pattern("snapshot_{VERSION}.md")
            .max_single_file_size(cli.max_file_size);

        let saved_path = snapshot.generate_and_save(&results, &fmt)?;
        println!("✅ Zapisano raport do: {}", saved_path.display());
    }

    Ok(())
}
```

## End Structure Summary

```plaintext
 ▣─┬ cargo-plot@1                   [6.3 KiB / 0 B]                    A:/A-JAN/WIN-DOCS/REPO_OWN_RUST/GIT 
   │                                                                   _j-Cis/cargo-plot@1/                
   └──┬ src                         [    0 B / 0 B]                                                        
1     ├──• cli.rs                   [      2.6 KiB] [2026-09-21 01:11] ./src/cli.rs                        
2     └──• main.rs                  [      3.7 KiB] [2026-09-21 01:11] ./src/main.rs                       
```

---
*Generated automatically by querypath-snapshot*
